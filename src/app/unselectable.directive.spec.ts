import { UnselectableDirective } from './unselectable.directive';

describe('UnselectableDirective', () => {
  it('should create an instance', () => {
    const directive = new UnselectableDirective();
    expect(directive).toBeTruthy();
  });
});
